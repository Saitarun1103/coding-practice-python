# coding-practice-python
python(innomatics )
task2->
📚 Notebook ContentsThe notebook (task2) walks through the following 30 text processing tasks:  
1. Username Cleaning System: Uses strip() and capitalize() to format raw user input.   2. File Name Analyzer: Checks if a file ends with .pdf using endswith() and extracts the filename using slicing.   3. Text Length and Count: Calculates total characters using len() and counts specific letter occurrences using count().   4. Search Position: Uses find() to locate the first occurrence of a character and demonstrates how it returns -1 when a character is not found.   5. Replace Scenario: Swaps specific words in a string using the replace() method.   6. Case Conversion: Demonstrates converting mixed-case strings to fully lowercase and uppercase.   7. Title Formatting: Capitalizes the first letter of each word in a string using title().   8. Split Operation: Breaks a comma-separated string into a list of individual elements using split().   9. Join Operation: Combines a list of strings into a single string separated by spaces using join().   10. Indexing Practice: Retrieves the first character, the last character, and a range of characters from a string using positive and negative indexing.   11. Slicing Challenge: Extracts a specific substring from a larger word using slice notation.   12. Reverse String: Reverses a string using step slicing ([::-1]).   13. Step Slicing: Extracts characters at specific intervals using a step value in slice notation ([1:8:2]).   14. Reverse Words Logic: Reverses an entire multi-word string.   15. Strip vs Replace: Compares removing leading/trailing spaces with strip() versus removing all spaces with replace().   16. Startswith / Endswith: Evaluates string prefixes and suffixes to return boolean values.   17. Character Type Check: Evaluates strings using isalnum() and isalpha() to determine if they contain alphanumeric or strictly alphabetic characters.   18. Uppercase Check: Validates if a string is entirely uppercase using isupper().   19. Lowercase Check: Validates if a string is entirely lowercase using islower().   20. Digit Check: Validates if a string consists only of numbers using isdigit().   21. Immutability Check: Demonstrates that strings are immutable in Python by attempting (and failing with a TypeError) to reassign a specific character via its index.   22. Replace Trap: Highlights the case-sensitive nature of the replace() method.   23. Count vs Find: Compares count(), find(), and rindex() for analyzing character occurrences and positions.   24. Find vs Index Difference: Illustrates the difference in error handling when a substring is missing: find() returns -1, while index() throws a ValueError.   25. Membership Check: Uses the in operator to check for a substring and index() to locate it.   26. Email Processing: Splits an email address at the @ symbol to extract the username and domain separately.   27. Word Count Logic: Combines split() and len() to count the number of words in a sentence.   28. Data Cleaning Pipeline: Chains methods to strip whitespace and split a comma-separated string into a clean list.   29. Password Validator: Uses isalnum() to check if a password string meets basic alphanumeric requirements.   30. Modify Given Text: Creates a multi-step pipeline that strips whitespace, converts text to uppercase, and replaces specific words.   🛠️ Technologies UsedLanguage: Python 3.13.9   Environment: Jupyter Notebook (IPython)   🚀 How to UseClone this repository to your local machine.Open the notebook using Jupyter Lab, Jupyter Notebook, or a supported IDE like VS Code.Run the cells sequentially to execute the string manipulation examples and observe the outputs and exceptions.
